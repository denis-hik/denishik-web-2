import styled from "styled-components";

export const EquinePanelStyled = styled.div`
    width: calc(100vw - 10%);
    height: 100%;
    display: block;
    
    .item-wrapper {
        
    }
    *, :before, :after {
        box-sizing: border-box;
    }

    padding-top: 10vh;
`